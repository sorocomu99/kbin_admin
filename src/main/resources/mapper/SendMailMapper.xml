<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kb.inno.admin.DAO.SendMailDAO">
    <select id="selectPageCount" parameterType="String" resultType="int">
        /* 보낸메일 갯수 조회 */
        SELECT COUNT(*)
          FROM KB_SEND_MAIL
         WHERE 1 = 1
        <if test='keyword != null and keyword != ""'>
            <choose>
                <when test="type == 'title'">
                    AND MAIL_TTL LIKE '%'|| #{keyword} ||'%'
                </when>
            </choose>
        </if>
    </select>

    <select id="selectList" resultType="com.kb.inno.admin.DTO.SendMailDTO">
        /* 보낸 메일함 리스트 */
        SELECT SEND_YMD
             , SEND_MAIL_SN
             , MAIL_RCVR
             , MAIL_TTL
             , MAIL_CN
             , ROWNUM ROWNUMBER
          FROM (SELECT SEND_YMD
                     , SEND_MAIL_SN
                     , MAIL_RCVR
                     , MAIL_TTL
                     , MAIL_CN
                  FROM KB_SEND_MAIL A
                 WHERE 1 = 1
                <if test='keyword != null and keyword != ""'>
                    <choose>
                        <when test="type == 'title'">
                            AND MAIL_TTL LIKE '%'|| #{keyword} ||'%'
                        </when>
                    </choose>
                </if>
                 ORDER BY SEND_YMD DESC, SEND_MAIL_SN ASC
               )
         WHERE ROWNUM BETWEEN #{start} AND #{end}
    </select>
</mapper>